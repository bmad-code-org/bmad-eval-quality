name: PR Checks

on:
  pull_request:
    branches: [main]

jobs:
  lint-typecheck-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Lint architecture spine
        run: npm run lint:spine

      - name: Test spine linter
        run: pip install pytest && python -m pytest scripts/spine-lint/tests -q

      - name: Test
        run: npm run test

      - name: Build
        run: npm run build
