---
title: 'The published surface, the corpus, and the census'
type: 'feature'
created: '2026-09-09'
status: 'draft'
review_loop_iteration: 0
context:
  - _bmad-output/implementation-artifacts/epic-11-context.md
  - _bmad-output/implementation-artifacts/11-4-the-operation-shape-for-a-tool-call.md
  - _bmad-output/implementation-artifacts/11-5-compile-and-preflight-admit-an-mcp-interface.md
  - _bmad-output/implementation-artifacts/11-6-the-tool-call-defect-signature-and-the-ninth-input-channel.md
  - _bmad-output/implementation-artifacts/11-13-the-port-messages-and-the-mcp-adapter.md
  - _bmad-output/implementation-artifacts/11-7-the-third-conformance-arm-and-the-graded-kind.md
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** Four stories changed the language and every enumeration a generator owns still counts to the old total. `corpus/dev/` ships 21 contracts (`corpus/dev/index.json` carries 24 entries: 22 of kind `contract`, one `sealed-evaluator-brief`, one `readme`), and 18 of the 21 declare an `api` interface, 2 declare `cli`, and `no-operation-inventory.json` declares none. Not one describes a tool server, so the kind this epic opened has no published example and nothing has ever run `score` over an `mcp` sealed run record. `tests/schemas/published-census.ts` pins six constants over the published schemas; Stories 11.4 and 11.6 moved four of them and this story is where all six are read back against the tree. `scripts/dev-corpus-target.ts` hand-writes six spelled-out numerals across four lines, `:65`, `:108`, `:114` twice and `:115` twice, and this story's diff falsifies four of them at their source. Six sentences under `docs/` and `README.md` hand-write the same corpus count and `scripts/check-docs.mjs:9-15` still does not scan `docs/`, so nothing gates them.

**Approach:** Promote Story 11.4's `mcp` fixture into `DEV_CORPUS_CONTRACTS`, run every generator, and take that exemplar end to end through `compile`, `seal`, `preflight` and `score` so the kind's last unrun stage has a run behind it. The pinned numbers are read against the post-11.6 state: the corpus manifest and the corpus README are the only generated bytes this story moves, and the six census constants carry the values Stories 11.4 and 11.6 left, each re-read and recorded here with the story that moved it. Then correct every hand-written count this story falsifies, in the same diff, at its source, and ship the script that gates fourteen of them so the next epic finds none of them by grep.

## Boundaries & Constraints

**Always:**

- Regenerate through the `generate:` scripts. A generated file is never hand-edited. `corpus/dev/README.md`, `corpus/dev/index.json`, `schemas/*.schema.json`, `docs/ad21-*.generated.md`, `docs/ad31-*.generated.md`, `docs/ad33-*.generated.md`, the worked chain's five files, and `_bmad-output/shareable/*.html` each come from their own generator and their own `check:` script proves it.
- Prose edits land at the template, so `corpus/dev/README.md`'s text is edited in `scripts/dev-corpus-target.ts` and reaches disk through `npm run generate:dev-corpus`.
- Every pinned number is read from the failure that names it, and a number this story leaves alone is recorded with the value it holds and the story that put it there. `published-census.ts:8-13` records why the six stay pinned integers: a floor cannot catch a narrowed walk, and dropping `propertyNames` from the occurrence descent silently removes 28 occurrences while every other assertion passes.
- `check:boundary` scans `src/`, `schemas/`, `corpus/`, and three `package.json` fields against twelve forbidden patterns (`BOUNDARY_PATTERNS`, `scripts/package-boundary.ts:56-105`), and `story`, `epic`, `AC n`, task numbers and story-local decision numbers are five of them. The corpus README is generated into `corpus/`, so its template text in `scripts/dev-corpus-target.ts` is in scope.
- Documentation moves in the same diff as the change that falsifies it. Every hand-written count the corpus and the census falsify is corrected here, with the current sentence, its line, and its replacement named in the Code Map.
- Every `.describe()`, JSDoc and comment this story writes gets the voice pass while it is written. No comment runs longer than the declaration it documents. After editing, grep the edited files for `, not `, `rather than`, `instead of`, `as opposed to`, `, never `, `no longer` and keep only the hits where both halves carry a fact.
- Updated is the floor. Text this change makes redundant is cut in the same pass.
- A caller-facing change is disclosed in `CHANGELOG.md` under `[Unreleased]` in the same diff, per NFR8 (`epics.md:54`) and the file's own header at `:7-9`. Nothing below `[Unreleased]` is touched: `npm run release:prepare` (`package.json:111`) is what moves a section into a dated heading.
- The learning-path step is written after the peer review's findings are addressed and before the human reviews locally, following `_bmad-output/project-knowledge/learning-path-template.md`.

**Ask First:**

- Any `src/` behaviour change. Every behaviour this story regenerates over shipped in Stories 11.3 through 11.7, and a behaviour found missing here is a finding against one of them.
- Any `schemaVersion` move. Story 11.4 owns the eval contract's breaking bump and the probe's first; Story 11.6 owns the probe's second and the sealed run record's. This story restates all four and moves none.
- A second compile-and-seal example. `EXAMPLE_SEED_ID = 'satisfied-declarations'` (`scripts/dev-corpus-target.ts:69`) is what `scripts/check-doc-invocations.mjs` replays every documented command against, so a second example doubles that surface.

**Never:**

- No hand-edit of a generated file. A wrong byte in `corpus/dev/README.md` is repaired in `scripts/dev-corpus-target.ts` and regenerated.
- No number asserted from memory. Every count in this story's diff is read off the artifact or off the failing assertion at the time it is written.
- No spine edit, no spine revision, and no new ADR. Story 11.5 owns `ARCHITECTURE-SPINE.md:232`, `:288` and `:656`, because opening the kind is what falsifies all three. This story reads them and asserts they already say what the shipped behaviour does.
- No published documentation prose beyond the counts. The index block at `docs/index.md:70-84`, the What Ships tool-use section, the tool-use guide rewrite and its `:242` are Story 11.9's; `docs/how-to/author-behavioral-contracts.md:148`'s observation-`kind` sentence is Story 11.13's; `docs/reference/glossary.md:91` and `docs/explanation/what-ships.md:18` are Story 11.5's; `docs/how-to/evaluate-ai-feature-behavior.md:14` is Story 11.4's. Anything prose-shaped found here is handed to its owner by name.
- No widening of `scripts/check-docs.mjs`'s `ROOTS`. That script checks frontmatter shape and whitespace, and pointing it at `docs/` would apply the planning-artifact whitespace rules to the published site while still reading no count. The count gate is its own script, per Decision 7.
- No rewriting of published prose from spelled-out numerals to digits. The docs spell every count as a word, and the gate reads the word.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| `generate:dev-corpus` then `check:corpus` | `DEV_CORPUS_CONTRACTS` with one `mcp` exemplar | 22 contracts plus the compile-and-seal pair; `index.json` carries 25 entries, 23 of kind `contract`; zero orphans, zero drift | Any orphan or first-differing-offset report blocks |
| The `mcp` corpus contract | `tests/schemas/fixtures/mcp-contract.ts`, authored by Story 11.4 beside `command-contract.ts` | Compiles clean through the shipped `compile`, so it publishes with no `structuralFailure` entry | A structural failure would raise the pinned count at `tests/architecture/dev-corpus.test.ts:333` from 3 and is investigated before it is accepted |
| The exemplar through all four stages | `compile`, `seal`, `runPreflight` against a hand-written fake port, `runScore` over a sealed run record naming the exemplar | Every stage returns, `score` yields a ladder with a non-null verdict, and the run is the first time `score` reads an `mcp` sealed run record | A refusal at any stage names the code and the artifact path and is a finding against the story that shipped that stage |
| `generate:schemas` then `check:schemas` | The published documents after Stories 11.4 and 11.6 | Exit 0 with nothing to regenerate, because both stories regenerated and committed in their own diffs | A byte that moves here names a story that shipped a stale `schemas/` tree and is reported as such. `check:schemas` is a script because AD-30 forbids test filesystem I/O outside a temp dir (`scripts/check-schemas.ts:6-9`) |
| `CENSUS_BY_DOCUMENT`, `CENSUS_BY_KEYWORD`, `CENSUS_TOTAL` | The keyword walk over the twelve documents, after Story 11.6's probe and sealed-run-record changes | All three assertions green on the values Story 11.6 left, read off `published-census.ts:20-33`, `:36-56` and `:64` and recorded in this story's decisions | A failure at `keyword-mutation.test.ts:135-137` names which of the three moved and means Story 11.6 shipped without following the procedure at `published-census.ts:15-17`; the sums at `:138-143` catch an arithmetic slip |
| `DEFS_BY_DOCUMENT` | `publish.test.ts:87-104`, pinned per document | Green on the value Story 11.6 left. `probe` moves there if `McpDefectSignature` carries `.meta({ id })`, the way `Operation` at `interface.ts:137-141` does | A positional `$defs` name (`__schema0`) fails the same test and is a schema defect |
| `ACCEPT_FIXTURE_COUNTS.unionBranches` and `.distinctInstances` | `UNION_BRANCH_FIXTURES` (`artifact-fixtures.ts:1109-1161`), 8 entries and 22 distinct instances at 1.4.2 | Story 11.4 lands two seeds and takes them to 10 and 24; Story 11.6's `DefectSignature` seed starts from 10 and 24. This story adds no seed, edits neither constant, and records the pair it read | `published-rejection.test.ts:219` and `:236-238` fail if either story landed a branch without its seed, which `keyword-mutation.test.ts:190-193` reports first as an unprotected occurrence |
| `REJECT_CASE_COUNTS` | `contract: 55`, `artifact: 96`, `total: 151` at 1.4.2 | Whatever Stories 11.4 and 11.6 added by AD-13; the three are re-read and the sum re-agrees. This story edits one existing case's `constraint` prose and adds none | `published-rejection.test.ts:94-96` and `differential.test.ts:32-33` both fail if the sum breaks |
| `check:ad31-table` | `generate-ad31-table.ts:16-19` reads `CORPUS_CONTRACTS`, which stays at 19 | No regeneration, and this story asserts that as an acceptance criterion | A moved table means the exemplar landed in the wrong array |
| `check:worked-example` | The chain after Stories 11.4 and 11.6 regenerated it | Exit 0 with no regeneration needed; `probe.json:35` still reads `"interfaceKind": "api"` | A needed regeneration names a story boundary that shipped red and is reported as such |
| `check:ad5-registry` | `ARCHITECTURE-SPINE.md:232` as Story 11.5 left it | Exit 0, and the row's "Fires when" cell reads ``a declared interface kind is `web` ``. The script parses the first column only (`scripts/check-ad5-registry.ts:96`), so a human reading the row is what proves the prose | A cell still naming `cli` or `mcp` is a finding against Story 11.5 and is reported by name |
| The spine's two capability sentences | `ARCHITECTURE-SPINE.md:288` and `:656` after Story 11.5 | AD-10's rule sentence names the kinds v0 accepts, and `:656`'s "Bringing `mcp` into v0 remains Deferred." is gone | Either sentence surviving unchanged is a finding against Story 11.5 |
| `check:doc-counts` | The corrected pages and their sources of truth | Exit 0. A page whose numeral disagrees with its source names the file, the line, the word it carries, and the word it should carry | A pattern matching nothing is reported as a dead entry, so a rewritten sentence cannot silence the check |
| `npm run validate` then `npm run docs:build` | The finished tree | Both exit 0. `validate` runs 22 steps once `check:doc-counts` is wired in, and none of them is `docs:build` | N/A |

</frozen-after-approval>

## Code Map

**The census and its four consumers**

- `tests/schemas/published-census.ts` -- the six pinned constants and the procedure at `:15-17`. Read at 1.4.2: `CENSUS_BY_DOCUMENT:20-33` (twelve documents summing to 3023), `CENSUS_BY_KEYWORD:36-56` (nineteen keywords summing to 3023), `CENSUS_TOTAL:64` (3023), `DEFS_BY_DOCUMENT:67-80` (eval-contract 7, probe 7, evidence-artifact 3, sealed-run-record 4, evaluator-configuration 1, the other seven 0), `REJECT_CASE_COUNTS:88-92` (contract 55, artifact 96, total 151), `ACCEPT_FIXTURE_COUNTS:100-107` (accepts 12, probeClasses 4, qualificationRoutes 5, unionBranches 8, relevanceContracts 3, distinctInstances 22) with `ACCEPT_FIXTURE_TOTAL` derived at `:109-114` as 32. Every one of the six arrives at this story carrying a value Story 11.4 or Story 11.6 wrote, and this story re-reads all six and edits none. Decision 2 records the chain.
- `tests/schemas/published/keyword-mutation.test.ts` -- the sweep. `:119-144` asserts the full census across the twelve documents, `:135-137` is the three-way comparison that names which constant moved, and `:138-143` re-adds both maps against the total. `:190-193` is the assertion that fails on a published keyword no fixture flips, which is why a union branch and its seed ship in one diff. The sweep body at `:83-117` deletes each mutable keyword occurrence and requires a corpus verdict to flip; `SWEEP_TIMEOUT_MS` at `:38` is 240 s because eval-contract alone carries 1108 occurrences.
- `tests/schemas/published/published-rejection.test.ts` -- the union-branch census. `:94-96` pins the three reject-case counts, `:182-204` assembles `PUBLISHED_ACCEPT_FIXTURES` from the five lists, `:215-219` asserts the total, and `:222-238` pins the distinct-instance count with the alias reasoning. The comments at `:215-216` ("six union branches") and `:222` ("Thirty listings, twenty distinct instances") are stale against the constants they document and are corrected here.
- `tests/schemas/publish.test.ts` -- the document census. `:61-65` builds one document per registry key and pins `INTERCHANGE_ARTIFACT_KEYS` at twelve; `:87-104` walks every `$defs` name, rejects positional and underscore-prefixed names, and compares the per-document map to `DEFS_BY_DOCUMENT`; `:196-203` pins the ledger split.
- `tests/schemas/published/differential.test.ts` -- the Zod-to-JSON-Schema equivalence. `:28-34` counts every hand-written reject case across the twelve and asserts the total against `REJECT_CASE_COUNTS.total`.

**The fixtures**

- `tests/schemas/fixtures/artifact-fixtures.ts:1109-1161` -- `UNION_BRANCH_FIXTURES`, 8 entries at 1.4.2. Two of the eight are `cli`-only: `eval-contract/command-interface` at `:1110-1117`, whose comment says the `cli` branch's operation shape, input binding and witness leg spelling "are reachable from no other seed", and `probe/command-signature` at `:1136-1142`. Story 11.4 adds two, `eval-contract/mcp-interface` beside the first and its `mcp` probe accept fixture, and Story 11.6 adds the `DefectSignature` `mcp` seed beside the second. This story reads all three and adds none.
- `tests/schemas/published/corpus.ts:28-45` -- `seedsOf`, the five seed lists the census sums. Three are closed by their own semantics: `ARTIFACT_ACCEPT_FIXTURES` is one per artifact, `PROBE_CLASS_FIXTURES` is AD-9's classes, and `QUALIFICATION_ROUTE_FIXTURES` is AD-9's five routes, with the comment at `:38-41` recording why the fifth route needed naming. `RELEVANCE_CONTRACTS` is eval-contract only. So `UNION_BRANCH_FIXTURES` at `:45-47` is the one open list, which is why Story 11.4's `mcp` probe accept fixture lands there and why that story moves the pair by two.
- `tests/schemas/fixtures/artifact-fixtures.ts:480-489` -- `fragmentSelectionSignature`'s docblock, which states the rule the two seeds above satisfy: it is "the only seed that reaches those keywords: a branch nothing exercises is a branch AD-13's sweep reports as unprotected" (`:486-488`).
- `tests/schemas/fixtures/command-contract.ts:17` and `:191` -- `commandContract` and `artifactCommandContract`, the two `DEV_CORPUS_CONTRACTS` members whose reason for shipping is the interface kind, and the precedent the `mcp` exemplar joins them under.
- `tests/schemas/fixtures/mcp-contract.ts` -- the `mcp` exemplar itself, authored by Story 11.4 on `command-contract.ts`'s model with two distinct tools, a sensitivity witness and a structured-result descriptor, and used there as the `PermittedInterface` union-branch seed. Story 11.7 imports it. This story promotes it into `DEV_CORPUS_CONTRACTS` and writes no new fixture module.
- `tests/schemas/fixtures/reject-cases.ts:133-146` -- `interface-kind-outside-the-four`, constraint `'an interface kind is api, web, cli, or mcp'`, mutating `kind` to `'grpc'` and expecting `invalid_union` / `const` at `/permittedInterfaces/0/kind`. Its comment at `:139-141` explains that the discriminated union makes both sides report the discriminator.
- `tests/schemas/fixtures/artifact-reject-cases.ts:1268-1279` -- `brief-interface-kind-outside-the-four`, constraint `"an interface kind is AD-19's closed four"`, expecting `invalid_value` / `enum`.
- `src/core/schemas/interface.ts:264` -- `INTERFACE_KINDS = ['api', 'web', 'cli', 'mcp']`, four and staying four.

**The dev corpus**

- `tests/coverage/fixtures/corpus.ts:514-534` -- `CORPUS_CONTRACTS`, 19 entries, exactly the AD-31 cell contracts in cell order. It does not move.
- `tests/coverage/fixtures/corpus.ts:536-559` -- `DEV_CORPUS_CONTRACTS`, `CORPUS_CONTRACTS` plus `commandContract` and `artifactCommandContract`, 21 today. Its docblock at `:537-546` states the rule the `mcp` exemplar inherits: a published corpus that could not show the shape "would leave an adopter with no worked example of the shape this version opened". `:548-553` records that a kind is graded in its own coverage file, which Story 11.7 discharges.
- `scripts/generate-dev-corpus.ts:17` -- reads `DEV_CORPUS_CONTRACTS`; `:38` clears `corpus/dev/` before writing so a removed contract cannot linger.
- `scripts/generate-ad31-table.ts:16-19` -- reads `CORPUS_CONTRACTS` and `CORPUS_CELLS`, which is why the AD-31 table does not move.
- `scripts/dev-corpus-target.ts:65` -- `EXAMPLE_SEED_ID`'s docblock, "three of the twenty-one do not compile". "twenty-one" becomes twenty-two; "three" holds.
- `scripts/dev-corpus-target.ts:106-145` -- the README template, a template literal with no interpolation, so every numeral in it is a hand-typed word. Five of the six numerals the epic tracks sit here, at `:108`, `:114` twice and `:115` twice; the sixth is `:65`'s docblock above. `:108` "Twenty-one contracts and one compiled-and-sealed pair" becomes Twenty-two. `:114` "twenty-one contracts. Nineteen are one per discipline rule in each" becomes twenty-two, and "Nineteen" holds because it counts `CORPUS_CONTRACTS`, the AD-31 cell contracts, which this epic does not touch. `:115` "declaration state, and two describe a system under test that runs behind a command. Eighteen are" moves "Eighteen" to Nineteen and leaves "two" for Story 11.10, and the sentence gains a clause naming the tool-server contract. `:117` "Three fail compilation by design" holds, because the exemplar compiles.
- `corpus/dev/README.md:3` and `:9-12` -- the byte-checked mirror of those lines.
- `corpus/dev/index.json` -- 24 entries today: 22 `contract`, 1 `sealed-evaluator-brief`, 1 `readme`. The manifest cannot carry its own digest, so `index.json` is the one file on disk with no entry (`tests/architecture/dev-corpus.test.ts:209-213`).
- `tests/architecture/dev-corpus.test.ts:314-334` -- case 165, which parses every published contract and pins `structuralFailure` entries at exactly 3 (`:331-333`). `:267-293` is case 163, the README's four named absences, matched by regex across line breaks.
- `scripts/check-dev-corpus.ts` -- the orphan sweep over the whole `corpus/` tree and the first-differing-offset byte comparison.

**How the six numerals compose across the epic**

Six spelled-out numerals sit on four lines: `:65` "twenty-one", `:108` "Twenty-one", `:114` "twenty-one" and "Nineteen", and `:115` "two" and "Eighteen". `:117`'s "Three" holds throughout, because every exemplar the epic adds compiles. Four stories add dev-corpus members and the epic's execution order is 11.1 through 11.8, then 11.10 through 11.12, then 11.9, so this story lands the `mcp` exemplar first and Story 11.11 writes the last words.

The composition, one move per story, so no two stories claim the same edit:

| Story | Corpus total | Compiling | Per-kind count it moves | Numeral words it edits |
|---|---|---|---|---|
| 11.8, here | 21 to 22 | 18 to 19 | none; `mcp` arrives at 1 | `:65`, `:108`, `:114`'s "twenty-one", `:115`'s "Eighteen" |
| 11.10 | 22 to 23 | 19 to 20 | `cli` 2 to 3 | `:115`'s "two" |
| 11.11 | 23 to 24 | 20 to 21 | `api` 18 to 19 | none of the six beyond the total |
| 11.12 | no move | no move | none | none |

Final state after 11.11: 24 contracts, `index.json` at 27 entries with 25 of kind `contract`, the split `api` 19, `cli` 3, `mcp` 1, and one declaring none, 21 compiling and 3 failing by design, so `tests/architecture/dev-corpus.test.ts:333`'s pin of 3 survives the whole epic. `:114`'s "Nineteen" holds throughout, because it counts `CORPUS_CONTRACTS` and none of the four exemplars is a declaration-state cell contract.

This story therefore claims exactly four edits at their source and nothing past its boundary. `docs/how-to/evaluate-agent-behavior.md:294`'s "two contracts describing a system behind a command" is the published face of `:115`'s "two", it holds here, and Story 11.10 is what moves it, which is why Decision 7 gives it a gate entry rather than leaving it to story discipline.

**The worked chain**

- `scripts/worked-example-target.ts:73-79` -- `WORKED_EXAMPLE_FILES`, the five generated files; `FINDINGS.md`, `README.md`, and `system-under-test.md` are hand-authored and untouched.
- `scripts/worked-example-target.ts:1193-1312` -- how the chain is built: `compile` at `:1193`, `seal` at `:1194`, `sealProbeSet` at `:1222`, and `score` at `:1289`. It never calls `preflight`. `:1211` parses the authored `PreflightVerdict` from `:1134-1141`, whose docblock records that it is `score()`'s fourth positional parameter and the source of `preflightPassed`. So the chain covers three of the four stages this story has to demonstrate, and riding it would leave pre-flight unrun.
- `scripts/worked-example-target.ts:678-682` -- `SEEDED_SIGNATURE`, `interfaceKind: 'api'` with a method and a path template, homed on the read for the reason its docblock at `:670-677` gives.
- `_bmad-output/.../spike-worked-example/probe.json:35` -- `"interfaceKind": "api"`, the generated mirror of that line. The chain's `corpusDigest` at `evidence-artifact.json:172` is a placeholder, so the corpus gaining a member does not reach it.

**The AD registries and the spine**

- `scripts/check-ad5-registry.ts:96` -- `ROW_PATTERN = /^\s*\|\s*` + backtick + `([a-z0-9-]+)` + backtick + `\s*\|/`, the first column and nothing after it, so `Fires when` and `Cited by` are checked by no script.
- `ARCHITECTURE-SPINE.md:232` -- at 1.4.2 the row reads ``| `unsupported-interface-kind` | a declared interface kind is `web`, `cli`, or `mcp` | AD-10 |``. `cli` stopped firing it in Epic 9 and `mcp` stops in Story 11.5, which owns the edit because opening the kind is what falsifies the cell. This story reads the row and asserts it names `web` alone.
- `ARCHITECTURE-SPINE.md:656` -- at 1.4.2 it reads "Bringing `mcp` into v0 remains Deferred." Story 11.5 is what falsifies it and owns the edit; `:288`, AD-10's own rule sentence, moves in the same diff. This story reads both and asserts neither still defers the kind.
- `scripts/build-shareable.mjs:37-44` and `:60-67` -- `README.md` and `ARCHITECTURE-SPINE.md` are both projection sources into `_bmad-output/shareable/*.html`, and `check:shareable` is step 6 of `validate` (`package.json:113`). This story edits `README.md:180`, so `build:shareable` and `check:shareable` ride with it whether or not the spine moved.

**The published description strings that ride into `schemas/`**

- `src/core/schemas/interface.ts:137-141` -- `Operation`'s `.meta` description, "Carried by the `api`, `web`, and `mcp` branches alike, so the export names it once instead of inlining three copies that could drift apart". Story 11.4 owns the rewrite; this story greps for survivors.
- `src/core/schemas/eval-contract.ts:193` -- `permittedInterfaces`' description, which names the `api`, `web` and `mcp` branches as carrying the shipped operation shape unchanged and carries the AD-11 bump record. Story 11.4 owns it.
- `src/core/schemas/interface.ts:276` and `:281`, `:296`, `src/core/schemas/defect-signature.ts:152` and `:164`, `src/core/schemas/sensitivity-witness.ts:73`, `src/core/compile/interface-inventory.ts:28`, `src/core/seal/plan-index.ts:228` -- the eight other `mcp` lines under `src/`. `grep -rn mcp src/` returns 11 at 1.4.2: these eight, plus `interface.ts:140` and `eval-contract.ts:193` above and `interface.ts:264`'s `INTERFACE_KINDS`. Each is owned by the story that changes the behaviour it describes and each is re-read here as a completeness sweep.

**The disclosure**

- `CHANGELOG.md:7-9` -- "Write entries under `[Unreleased]`. `npm run release:prepare` moves them into a dated version section when a release is cut", so the whole edit is one section and nothing below it moves.
- `CHANGELOG.md:285-290` and `:311-313` -- Epic 9's artifact-bump bullets, the shape to copy: a **BREAKING** lead, the artifact and its new `schemaVersion`, what retyped, and what a consumer has to do about it. `_bmad-output/implementation-artifacts/9-5-the-published-surface-the-corpus-and-the-disclosed-breaks.md:127` is the one precedent for carrying it as a checklist item, and this story follows it.
- `CHANGELOG.md:578` -- the standing statement that "The `schemaVersion` number gates nothing in either direction in v0, so none of these breaks announces itself as a version mismatch." It is written once for the file and this story does not restate it.

**The hand-written counts this story falsifies**

Ungated today: `scripts/check-docs.mjs:9-15`'s `ROOTS` is `README.md` and four `_bmad-output` / `experiments` paths, and its only body work is whitespace repair at `:44-55`, so no count under `docs/` is read by it. `scripts/check-doc-invocations.mjs` replays fenced commands and judges exit codes. `grep -rn "twenty-one" tests/` returns nothing, so Story 9.5's Decision 4 test over these counts never landed. Every line below is corrected by reading, and each is quoted from the tree at 1.4.2. Decision 7 adds the gate that makes this the last epic that finds them by grep.

- `docs/reference/cli-commands.md:233` -- "`corpus/dev/` ships twenty-one contracts under `contracts/` ... Eighteen contracts compile. Three fail by design". Becomes twenty-two and Nineteen; the three named failures are unchanged.
- `docs/how-to/author-behavioral-contracts.md:94` -- "`corpus/dev/contracts/` holds twenty-one contracts: nineteen covering the seven discipline rules, one per declaration state, and two describing a system under test that runs behind a command. Eighteen compile, and three fail by design." Becomes twenty-two, with the tool-server contract named beside the two command ones, and Nineteen compile.
- `docs/how-to/evaluate-ai-feature-behavior.md:234` -- "Eighteen of the twenty-one contracts in `corpus/dev/contracts/` declare an `api` interface". Eighteen holds; twenty-one becomes twenty-two.
- `docs/explanation/what-ships.md:20` -- "a twenty-one-contract development corpus under `eval-quality/corpus/dev/`". Becomes twenty-two-contract. The tool-use section at `:36-42` is Story 11.9's.
- `docs/tutorials/getting-started.md:12` -- "The package ships twenty-one of them, so you do not have to write one yet." Becomes twenty-two. `:64`'s "three contracts that fail on purpose" holds.
- `README.md:180` -- "you can read twenty-one real contracts and one compiled-and-sealed pair without cloning this repository." Becomes twenty-two.

Checked and holding, listed so a later reader does not read the survival as a miss:

- `docs/how-to/author-behavioral-contracts.md:52` -- "requires twenty-one top-level fields" is the eval contract's field count. `schemas/eval-contract.schema.json` carries 21 properties and 21 required keys, and Story 11.4 adds no top-level field. This is the grep trap: it matches "twenty-one" and describes a different thing.
- `docs/how-to/evaluate-agent-behavior.md:294` -- "ships two contracts describing a system behind a command" stays two here, because the exemplar this story adds is `mcp`. Story 11.10 moves it to three, and Decision 7 gives it a gate entry now so that move is caught by the gate.
- `docs/how-to/author-behavioral-contracts.md:63` and `docs/reference/glossary.md:106` -- twelve schema documents and four declared interface kinds. Both hold.
- `docs/reference/cli-commands.md:223` (four reference adapters), `:227` (the conformance arms), `:229` (`CONFORMANCE_OUTCOME_COUNTS`: corpus 6, clock 6, file-system 12, environment-probe 19, command-probe 15). Stories 11.13 and 11.7 own these three under the same rule that gives this story the corpus counts. This story greps them and fails if either left one behind.

**The sources of truth the new count gate reads**

- `tests/coverage/fixtures/corpus.ts:555-559` -- `DEV_CORPUS_CONTRACTS.length`, the corpus total.
- `corpus/dev/index.json` -- entries of kind `contract` under `contracts/`, and the subset carrying `structuralFailure`, which give the compiling count and the failing-by-design count. The manifest is itself byte-gated by `check:corpus`, so the gate reads a value another gate already proved.
- The published contracts' own `permittedInterfaces[].kind` -- the per-kind split, 18 `api`, 2 `cli` and 1 declaring no interface today.
- `src/adapters/index.ts:12-21` -- the four `create*Adapter` factory exports (`createCommandLineAdapter` at `:13`, then the corpus, file-system and clock factories at `:17`, `:19` and `:21`) behind "four reference adapters".
- `src/testing/conformance.ts:40` -- `CONFORMANCE_OUTCOME_COUNTS`, the five digits at `cli-commands.md:229`.
- `src/core/schemas/artifact.ts:99` -- `INTERCHANGE_ARTIFACT_KEYS.length`, the twelve published schema documents.
- No numeral-to-word renderer exists in the tree. `scripts/dev-corpus-target.ts:106-145` is a template literal with no interpolation, so every numeral in it is a hand-typed word, and `src/core/coverage/table.ts:69` replaces hyphens in a state name. The gate ships its own closed word table for the range the docs use.

## Tasks & Acceptance

**Execution:**

- [ ] `tests/schemas/fixtures/mcp-contract.ts` -- read Story 11.4's exemplar and confirm it carries what the corpus needs: two distinct tools that do not collide under `duplicate-operation-signature`, Story 11.3's response descriptor, a nominated success indicator, and a sensitivity witness on every input-bearing operation. Every string in it clears `check:boundary`'s twelve patterns and AD-18's excluded content. A gap here is a finding against Story 11.4 and is fixed in that file with the finding recorded.
- [ ] `tests/coverage/fixtures/corpus.ts` -- promote `mcpContract` into `DEV_CORPUS_CONTRACTS` (`:555-559`) and leave `CORPUS_CONTRACTS` at 19. Extend the docblock at `:537-546` to say why a third kind ships without a cell, and cut whatever the extension makes redundant.
- [ ] `tests/schemas/fixtures/artifact-fixtures.ts` -- read `UNION_BRANCH_FIXTURES` and confirm all three `mcp` seeds landed: Story 11.4's `PermittedInterface` seed and its probe accept fixture, and Story 11.6's `DefectSignature` seed, each with a comment naming the keywords no other seed reaches, following `:486-488`'s rule and `:1110-1117`'s spelling. This story adds no seed. A missing seed is a finding against its story and shows up first as an unprotected occurrence at `keyword-mutation.test.ts:190-193`.
- [ ] `tests/schemas/published-census.ts` -- run the suite and read all six constants back. Expect no edit: `unionBranches` and `distinctInstances` at whatever Story 11.6's seed left, starting from the 10 and 24 Story 11.4's two seeds produced; `CENSUS_BY_DOCUMENT`, `CENSUS_BY_KEYWORD` and `CENSUS_TOTAL` at the values Story 11.6's probe and sealed-run-record changes left; `DEFS_BY_DOCUMENT` at whatever `McpDefectSignature`'s `.meta` produced; `REJECT_CASE_COUNTS` at whatever the two stories' AD-13 fixtures added; and `ACCEPT_FIXTURE_TOTAL` derived at `:109-114`. Record each read value with the story that moved it. A failure here means an earlier story shipped without following the procedure at `:15-17`, and the number is moved by reading the failure with that finding recorded.
- [ ] `tests/schemas/published/published-rejection.test.ts` -- correct the two stale comments at `:215-216` and `:222` against the constants they document, so the prose and the pinned numbers agree.
- [ ] `tests/schemas/fixtures/artifact-reject-cases.ts:1271` -- name the four kinds, matching `reject-cases.ts:135`'s spelling, so a reader of the case learns which four without opening AD-19. `reject-cases.ts:133-146` is re-read and its keyword, issue code and paths are confirmed unchanged.
- [ ] `scripts/dev-corpus-target.ts` -- move four of the epic's six numerals at their source: `:65`'s "twenty-one" and `:108`'s "Twenty-one" and `:114`'s "twenty-one" all to twenty-two, and `:115`'s "Eighteen" to Nineteen. Leave `:114`'s "Nineteen" and `:117`'s "Three", and leave `:115`'s "two" for Story 11.10, each for the reason in the Code Map's composition table. Describe the tool-server contract in "What is here", and cut the text the third kind makes redundant. This is published prose and gets the voice pass at its source.
- [ ] `npm run generate:dev-corpus && npm run check:corpus` -- regenerate; expect 25 index entries, 23 of kind `contract`, zero orphans, zero drift.
- [ ] `tests/architecture/dev-corpus.test.ts` -- confirm case 165's pinned 3 structural failures against the new total of 22 and case 163's four absence patterns against the rewritten README.
- [ ] `tests/application/mcp-end-to-end.test.ts` -- the exemplar through all four stages, per Decision 8. `compile` and `seal` over `mcpContract`; `runPreflight` with the exemplar, an `mcp` probe, and a hand-written fake port on `tests/preflight/fixtures/probe-port.ts`'s `echoPort` model; then `runScore` over a sealed run record naming the exemplar, following the six inputs `tests/application/fixtures/score-fixtures.ts` already assembles. Assert the pre-flight verdict passes and the score ladder carries a non-null contract verdict, which is what `worked-example-target.ts:1301-1304` fails the build on for the api chain. No filesystem I/O, per AD-30.
- [ ] `npm run generate:schemas && npm run check:schemas` -- expect nothing to regenerate, because Stories 11.4 and 11.6 each committed their own regenerated documents. A moved byte names the story that shipped a stale `schemas/` tree.
- [ ] `ARCHITECTURE-SPINE.md` -- read `:232`, `:288` and `:656` and confirm each says what the shipped behaviour does: the AD-5 "Fires when" cell names `web` alone, AD-10's rule sentence names the kinds v0 accepts, and `:656` no longer defers `mcp`. Story 11.5 owns all three edits; this story writes no spine byte and reports any survivor as a finding against 11.5.
- [ ] The six hand-written counts -- correct each at its source per the Code Map, then `npm run build:shareable && npm run check:shareable` for `README.md`'s projection.
- [ ] `scripts/check-doc-counts.ts` and `package.json` -- add the count gate and wire `check:doc-counts` into `validate`, per Decision 7. It holds one table entry per gated sentence: the file, a regex capturing the numeral, the expression that computes it, and the rendering. A pattern matching nothing is a dead entry and fails, so a rewritten sentence cannot silence the check. The new script name and value are scanned by `check:boundary` as `package.json#scripts.check:doc-counts` (`scripts/check-package-boundary.ts:109-111`).
- [ ] `CHANGELOG.md` `[Unreleased]` -- the disclosure, on `:285-290`'s shape and following the one precedent at `9-5-the-published-surface-the-corpus-and-the-disclosed-breaks.md:127`. It names two things: the dev corpus gaining a member, which moves the corpus digest and so stops a scoring version computed before this epic being comparable with one computed after, and the new `validate` step `check:doc-counts` adds. The non-comparability note is stated once for this epic and this is the story that states it, because this is the first story that moves the corpus. `:578`'s standing `schemaVersion` statement is left alone.
- [ ] `grep -rn mcp src schemas` -- confirm every surviving mention describes the shipped kind, and hand anything prose-shaped to Story 11.9 by name.
- [ ] Voice pass -- grep every file this story edited for `, not `, `rather than`, `instead of`, `as opposed to`, `, never `, `no longer`, and keep only the hits where both halves carry a fact.
- [ ] `_bmad-output/project-knowledge/learning-path-step-by-step.md` -- add this story's step as the next number after the highest in the file, marked `(epic11-story8)`, plus its table row, following `learning-path-template.md`. Written after the peer review's findings are addressed.

**Acceptance Criteria:**

- Given `npm run check:corpus` after regeneration, when it runs, then it reports zero orphans and zero drift over 22 contracts plus the compile-and-seal pair, and `index.json` names the `mcp` contract with a digest over the exact bytes on disk.
- Given the `mcp` corpus contract, when `compile` runs over it, then it returns with no `structuralFailure` entry, so `tests/architecture/dev-corpus.test.ts:333` still reads 3.
- Given the `mcp` dev-corpus exemplar, when it is put through `compile`, `seal`, `runPreflight` and `runScore` in one test, then every stage returns, the pre-flight verdict passes, and the score ladder carries a non-null contract verdict. This is the epic's definition-of-done clause that no earlier story's acceptance runs, because no other story runs `score` over an `mcp` sealed run record.
- Given each of the six census constants, when the suite runs, then every assertion in `keyword-mutation.test.ts:119-144`, `published-rejection.test.ts:94-96` and `:215-238`, `publish.test.ts:87-104`, and `differential.test.ts:28-34` passes with no constant edited by this story, and this story's decisions record each of the six with the value read and the story that put it there.
- Given `npm run check:ad31-table`, `check:ad21-table` and `check:ad33-table`, when they run, then none needed regeneration, proving no coverage cell, ladder row or outcome cell moved in a story that touched none.
- Given `npm run check:worked-example`, when it runs, then it exits 0 with no regeneration, and `probe.json:35` still reads `"interfaceKind": "api"` because the chain's subject is the api contract at `worked-example-target.ts:678-682`.
- Given `ARCHITECTURE-SPINE.md` as Story 11.5 left it, when `:232`, `:288` and `:656` are read, then the AD-5 row fires on `web` alone, AD-10's rule sentence names the kinds v0 accepts, and no sentence still defers `mcp`, with this story writing no spine byte. `npm run check:ad5-registry`, `npm run lint:spine` and `npm run check:shareable` are all green, the last over the `README.md` projection this story does move.
- Given each of the six documentation sentences named in the Code Map, when read, then each states the count the regenerated corpus carries, and each of the four checked-and-holding sentences is unchanged with its reason recorded here.
- Given every `.describe()`, JSDoc, comment and README-template line this story wrote, when the six banned constructions are grepped for, then every surviving hit is a before/after contrast where both halves carry a fact, and no comment runs longer than the declaration it documents.
- Given `npm run check:doc-counts`, when it runs over the corrected pages, then it exits 0 over its fourteen numerals across seven pages and reports how many entries it checked; and when any one of the gated numerals is flipped by hand, then it exits non-zero naming the file, the line, the word carried and the word owed. Its coverage list and its stated uncovered list are both in Decision 7.
- Given `CHANGELOG.md`, when the `[Unreleased]` section is read, then it carries this story's entry naming the corpus digest move and its effect on scoring-version comparability, and the added `validate` step; and when `git diff CHANGELOG.md` is read, then it touches nothing below `[Unreleased]`.
- Given `npm run validate`, when it runs, then it exits 0 with nothing on stderr across all 22 steps, one more than the 21 it runs today because this story wires in `check:doc-counts`. Given `npm run docs:build`, when it runs separately, then it exits 0. `docs:build` is `node tools/build-docs.mjs` and appears nowhere in `validate`'s chain.

## Decisions settled by construction

**Decision 1: the two `interface-kind-outside-the-four` cases stay at four, and the one edit is a spelling.**
`INTERFACE_KINDS` (`interface.ts:264`) is `['api', 'web', 'cli', 'mcp']` and this epic changes what `compile` admits, so the enum stays four while admission goes from two kinds to three. Both reject cases assert the enum: `reject-cases.ts:133-146` mutates `kind` to `'grpc'` against a discriminated union whose four branches each report their own `const`, and `artifact-reject-cases.ts:1268-1279` mutates the brief's kind against the bare enum and reports `invalid_value` / `enum`. Story 11.4 gives `mcp` its own branch shape, which leaves four branches carrying three operation shapes, so a fifth value still matches no branch and both keyword expectations hold. The epic register expected the prose corrected; the correction the tree actually needs is at `artifact-reject-cases.ts:1271`, whose `"an interface kind is AD-19's closed four"` counts the kinds without naming them. It gets `reject-cases.ts:135`'s spelling. Downstream consequence: a later story opening `web` finds two cases that already name their four and moves neither.

**Decision 2: this story moves no census constant, and its job on all six is to read them back and record who moved each.**
The chain is settled upstream and this story is its last link. `ACCEPT_FIXTURE_COUNTS.unionBranches` is 8 at `published-census.ts:104` and `.distinctInstances` is 22 at `:106` in the tree at 1.4.2. Story 11.4 ships two seeds: the `PermittedInterface` `mcp` seed with the branch that needs it, and its `mcp` probe accept fixture, which lands here because `seedsOf` (`tests/schemas/published/corpus.ts:28-45`) closes three of its five lists by their own semantics and leaves `UNION_BRANCH_FIXTURES` as the only open one. That takes the pair to 10 and 24. Story 11.6 ships the `DefectSignature` `mcp` seed with `McpDefectSignature` and starts from 10 and 24. Each seed is a new object and `published-rejection.test.ts:222-234` counts by identity, so the pair moves in step. No seed may land apart from its branch, because `keyword-mutation.test.ts:190-193` fails on any published keyword no fixture flips, which would leave `npm run validate` red at that story's boundary. `ACCEPT_FIXTURE_TOTAL` is derived at `:109-114`, so it takes no edit at any point in the chain.

This story predicts no post-11.6 value for the pair. It reads what the run reports and records it with Story 11.6 named, which is the same discipline `published-census.ts:15-17` writes down for the constants themselves.

The other four move the same way and for the same reason. `CENSUS_BY_DOCUMENT`, `CENSUS_BY_KEYWORD` and `CENSUS_TOTAL` are the keyword walk over `schemas/*.schema.json`, so they move whenever a published document's bytes move: Story 11.4 for the eval contract and the probe's first bump, and Story 11.6 for the probe's second bump and the sealed run record's, which is `McpDefectSignature` plus `ProbeInputBinding`'s ninth `arguments` channel plus the ninth `arguments` key on `ObservedCallInputs`. `DEFS_BY_DOCUMENT` moves at `probe` if `McpDefectSignature` carries `.meta({ id })`, which is Story 11.6's to record. `REJECT_CASE_COUNTS` moves with whatever AD-13 negative fixtures those two stories added.

This story adds one dev-corpus fixture and one test. Neither reaches `schemas/`, neither adds an accept fixture or a reject case, and the one edit it makes inside a reject case is the `constraint` prose at `artifact-reject-cases.ts:1271`. So the expected outcome is that all six read back green with no edit, and every value is recorded here with the story that wrote it. Downstream consequence: a red census at this boundary is a finding against Story 11.4 or Story 11.6 for shipping without following the procedure at `published-census.ts:15-17`, and this story moves the number by reading the failure and names the story in the same breath.

**Decision 3: the exemplar joins `DEV_CORPUS_CONTRACTS` and stays out of `CORPUS_CONTRACTS`, so the AD-31 table does not move.**
`scripts/generate-ad31-table.ts:16-19` reads `CORPUS_CONTRACTS` and `CORPUS_CELLS`, and `corpus.ts:509-513` records that both are written out because "derived, fixture 158 could not fail". `CORPUS_CONTRACTS` is exactly the AD-31 cell contracts in cell order, and an `mcp` exemplar is not a declaration-state exemplar, so adding it there would break the ordering check that proves the array derived. It goes where the two command contracts went, and `corpus.ts:537-546` already carries the reason. AD-31 grading for the kind is Story 11.7's dedicated coverage file, per the rule at `corpus.ts:548-553`. Downstream consequence: `check:ad31-table` expecting no regeneration is an assertion in this story, and a moved table means the exemplar landed in the wrong array.

**Decision 4: the worked chain is checked and not regenerated, and the reason is which contract it proves.**
`probe.json:35` mirrors `SEEDED_SIGNATURE` at `worked-example-target.ts:678-682`, whose `interfaceKind` is `'api'` and whose docblock at `:670-677` homes the signature on the read because the update's own response "is indistinguishable from a correct one". This epic adds a kind to the language and leaves the chain's subject where it is, so the value stays `api`. The chain's bytes move on a `schemaVersion` stamp and on the digests below it, and those moves belong to Story 11.4 and Story 11.6, each of which keeps `check:worked-example` green at its own boundary because that check sits inside `npm run validate`. The corpus growth does not reach the chain: `evidence-artifact.json:172` carries a placeholder `corpusDigest`, so the real corpus digest never enters it. This story therefore runs `check:worked-example` and expects exit 0 with nothing to regenerate, and a needed regeneration is reported as a story boundary that shipped red.

**Decision 5: the three spine lines are read here and edited by Story 11.5, and this story is the check on that edit.**
`scripts/check-ad5-registry.ts:96` matches the first column and stops, which is deliberate: it binds `src/core/failure-codes.ts` to the code list by set and order. Everything after the first pipe is prose no script reads, and `ARCHITECTURE-SPINE.md:232` is the proof: it has read ``a declared interface kind is `web`, `cli`, or `mcp` `` since Epic 9 opened `cli`, and one full release shipped with it. The kind list in that cell, AD-10's rule sentence at `:288`, and the deferral at `:656` are all falsified by the same event, which is Story 11.5 opening compile and pre-flight to `mcp`. The rule this epic runs on is that documentation moves in the diff that falsifies it, so all three are Story 11.5's and none is this story's. No revision number moves and no ADR opens.

What is left here is the check, and it is worth having precisely because no script performs it. This story reads all three lines and asserts each says what the shipped behaviour does, and a survivor is reported as a finding against Story 11.5 by name. That is the same shape Decision 6 gives the six published counts and Story 11.9 gives the whole documentation sweep: an owner per line, then one pass that catches a line nobody moved. Downstream consequence: whoever opens `web` has the last term to remove from `:232` and the row goes empty of kinds, which is the point at which the code itself becomes unfireable and the row is deleted with it.

**Decision 6: this story owns every count the corpus and the census falsify, and every other line has a named owner elsewhere.**
The split is by cause. A sentence stating a number this story's diff changes is false the moment the diff lands, so it moves in the same diff. That puts `cli-commands.md:233`, `author-behavioral-contracts.md:94`, `evaluate-ai-feature-behavior.md:234`, `what-ships.md:20`, `getting-started.md:12` and `README.md:180` here. A sentence describing what the product can be pointed at is falsified by the epic as a whole and has no single owning change, so it goes to Story 11.9 with the tool-use guide rewrite: the `docs/index.md:70-84` block, the whole of `evaluate-tool-use-behavior.md` including its `:242`, and `what-ships.md:36-42` after Stories 11.3 and 11.5 have taken their sentences out of it. The rest go to the story whose change falsifies them, per the ownership table at `epic-11-context.md:123-133`: `glossary.md:91` and `what-ships.md:18` to Story 11.5, `evaluate-ai-feature-behavior.md:14` to Story 11.4, `author-behavioral-contracts.md:148` to Story 11.13, and `cli-commands.md:223`, `:227` and `:229` to Stories 11.13 and 11.7. This story greps those last three lines and fails if either story left one behind. Decision 7 supplies the gate that makes the split enforceable in both directions.

**Decision 7: the count gate is a script wired into `validate`, it covers fourteen numerals across seven pages, and the uncovered set is named.**
Story 9.5's Decision 4 chose a Vitest test over a checker script for one number and the test never landed: `grep -rn "twenty-one" tests/` returns nothing, and six sentences have carried a hand-written corpus count through two epics since. The mechanism is reversed here on the source. AD-30's rule is that "no test performs filesystem I/O outside a temporary directory", with one carve-out named in the same sentence for AD-37's loopback suite, and every filesystem-reading gate this repository already runs is a script for that reason: `check:schemas` (`scripts/check-schemas.ts:6-9` says so explicitly), `check:docs`, `check:doc-invocations`, `check:corpus`, `check:worked-example`, and the five AD-table checks. A count gate reads committed markdown, so it is a script, and it is wired into `validate` beside its siblings. The one place a test already reads `docs/` is `tests/architecture/dev-corpus.test.ts:244-264`, whose `contractDigest` sweep walks the whole directory; that is an existing exception this story declines to widen, and it is named here so the next reader knows it was seen.

The rendering question has one answer the tree forces. No numeral-to-word helper exists: `scripts/dev-corpus-target.ts:106-145` is a template literal with no interpolation, so its five spelled-out numerals are hand-typed, and `src/core/coverage/table.ts:69` is a hyphen-to-space replace on a coverage state. So the check ships a closed word table for the range the docs use, computes the value from its source, renders it, and compares against a captured group. The pages keep their words.

Covered, counted so the arithmetic is checkable: the corpus contract total at `README.md:180`, `what-ships.md:20`, `cli-commands.md:233`, `author-behavioral-contracts.md:94`, `evaluate-ai-feature-behavior.md:234` and `getting-started.md:12` is six; the compiling count and the failing-by-design count at `cli-commands.md:233` and `author-behavioral-contracts.md:94` is four; the `api`-declaring count at `evaluate-ai-feature-behavior.md:234` is one; the `cli`-declaring count at `evaluate-agent-behavior.md:294` is one; the reference-adapter count at `what-ships.md:20` and `cli-commands.md:223` is two. Fourteen numerals across seven pages, plus one further entry capturing the five `CONFORMANCE_OUTCOME_COUNTS` digits at `cli-commands.md:229` in a single pattern. Every one has a source of truth already under another gate.

`evaluate-agent-behavior.md:294`'s "two contracts describing a system behind a command" is the fourteenth and it earns its entry in this story, ahead of the move it will have to catch. It reads two at this story's boundary and Story 11.10 moves it to three, so shipping the entry now means the gate is what catches that move. Every corpus numeral the epic touches is then held by the gate from the first story that touches the corpus, which is the whole point of building the gate in the same story that grows the corpus.

Uncovered, stated so the coverage is not read as complete: every other numeral under `docs/`, which includes the twelve outcome states, the fourteen AD-31 predicates, the seven discipline rules, the four commands, the six pre-flight verdict kinds, the four interface kinds at `glossary.md:106`, the twenty-one eval-contract top-level fields at `author-behavioral-contracts.md:52`, and the evidence claims at `index.md:76-77` ("Nine probes", "eight contracts") whose source of truth is an experiment record outside this tree. Also uncovered: any prose claim that is not a count, and anything the website build renders. Downstream consequence: a later story adding a gated numeral to a page adds a table entry, and a later story adding an ungated one inherits this list as the record of what it is joining.

**Decision 8: the exemplar gets its own four-stage run as a test, and the reason is what the two published chains actually do.**
The epic's definition of done says an `mcp` contract compiles, seals, pre-flights and scores. Compile and seal are already exercised over every corpus member by `scripts/generate-dev-corpus.ts`, which is how `index.json` gets a `structuralFailure` entry or does not. Story 11.13 drives `runPreflight` end to end against its adapter. Nothing anywhere runs `score` over an `mcp` sealed run record, and this story ships the exemplar, so this story takes the clause.

Three places could carry it and two are refused on what the tree says. The worked chain at `scripts/worked-example-target.ts` calls `compile` at `:1193`, `seal` at `:1194`, `sealProbeSet` at `:1222` and `score` at `:1289`, and it never calls `preflight`: `:1211` parses the authored `PreflightVerdict` from `:1134-1141`. So riding it would leave one of the four stages unrun, and retargeting `SEEDED_SIGNATURE` at `:678-682` off `api` would delete the epic-8 evidence the chain exists to carry, while adding a second chain doubles `WORKED_EXAMPLE_FILES` at `:73-79` and its byte check. The corpus's compile-and-seal example is one seed by design at `scripts/dev-corpus-target.ts:69`, and this story's Ask First already prices a second one: `scripts/check-doc-invocations.mjs` replays every documented command against it.

So it is a Vitest test beside the four application entry points it chains. AD-30 permits it, because `runPreflight` and `runScore` are in-process calls over authored values against a hand-written fake port, which is exactly what `tests/application/preflight.test.ts` and `tests/application/score.test.ts` already do, and neither touches the filesystem. It reuses `tests/preflight/fixtures/probe-port.ts`'s `echoPort` shape and the six inputs `tests/application/fixtures/score-fixtures.ts` assembles, so the cost is the fixture chain rather than a new mechanism. Downstream consequence: the next kind opened inherits a worked template for proving all four stages, and the ladder assertion is the same one `worked-example-target.ts:1301-1304` fails the build on, so a kind that scores to a null verdict cannot pass quietly.

## Design Notes

The organising idea is that a number nobody generates is a number that goes stale, and this repository has two answers to that. Where a generator owns the value, the fix is to move the input and rerun the generator, and the `check:` script turns a stale byte into a build failure: that covers `corpus/dev/README.md`, `index.json`, `schemas/*.schema.json` and the three AD tables. Where a human owns the value, the fix is a pinned constant whose failing assertion names which number moved, which is what `published-census.ts:8-17` buys and why its six stay integers.

The third category is the one this story spends most of its diff on: prose that states a number no mechanism reads. Fourteen published numerals and one spine table cell are in that category today, and the AD-5 row at `ARCHITECTURE-SPINE.md:232` shows what it costs, having carried a wrong kind list through a full release. Story 9.5 answered it with story discipline and a Vitest test that never landed, and two epics of drift is the measurement that argues against repeating the answer. So the fourteen counts get the third mechanism this repository already runs eleven of: a script that computes the value from its source, renders it, and fails the build on a mismatch. The spine cell keeps the story-discipline answer under Story 11.5's ownership, with this story's read as the second pair of eyes, because `check:ad5-registry` reads the first column by design and widening it to grade prose is a different check than the one AD-5 needs.

## Verification

**Commands:**

- `npm run generate:dev-corpus && npm run check:corpus` -- expected: exit 0, zero orphans, zero drift, 25 index entries with 23 of kind `contract`.
- `npm run generate:schemas && npm run check:schemas` -- expected: exit 0 with nothing to regenerate; twelve documents, no unexpected file under `schemas/`.
- `npm run check:ad31-table && npm run check:ad21-table && npm run check:ad33-table` -- expected: exit 0 with no regeneration for any of the three.
- `npm run check:ad5-registry` -- expected: exit 0 with the row parsed from its first column, and the "Fires when" cell read by hand as ``a declared interface kind is `web` ``.
- `npm run check:ad28-registry` -- expected: exit 0; no runtime fault code moved.
- `npm run check:worked-example` -- expected: exit 0 with nothing to regenerate.
- `npm run build:shareable && npm run check:shareable` -- expected: exit 0 after regenerating the `README.md` projection. The `ARCHITECTURE-SPINE.md` projection was regenerated in Story 11.5's diff and moves nothing here.
- `npm run lint:spine` -- expected: exit 0 over the spine as Story 11.5 left it.
- `npx vitest run tests/application/mcp-end-to-end.test.ts` -- expected: exit 0, with the exemplar through `compile`, `seal`, `runPreflight` and `runScore` and a non-null contract verdict on the ladder.
- `npm run check:boundary` -- expected: exit 0, with the authored `mcp` corpus contract and the regenerated README among the scanned `corpus/` entries.
- `npm run check:docs && npm run check:doc-invocations` -- expected: exit 0, with the tool-use guide's invocation still reported faithful.
- `npm run check:doc-counts` -- expected: exit 0 over fourteen numerals across seven pages, with the entry count printed. Flip one numeral by hand and expect a non-zero exit naming the file, the line, the word carried and the word owed.
- `npm run test:coverage` -- expected: exit 0, with every census assertion green against the moved constants.
- `npm run validate` -- expected: exit 0 with nothing on stderr across all 22 steps, `check:doc-counts` included.
- `npm run docs:build` -- expected: exit 0, run separately because `validate` does not build the website.

**Manual checks:**

- `grep -rn "twenty-one" docs README.md` returns only `docs/how-to/author-behavioral-contracts.md:52`, which counts the eval contract's 21 top-level fields.
- `grep -rn "mcp" src schemas` returns only descriptions of the shipped kind.
- `git diff CHANGELOG.md` -- every hunk sits inside `[Unreleased]`, and no dated heading below it moves.
- `ARCHITECTURE-SPINE.md:232`, `:288` and `:656` read against the shipped behaviour. Each says what `compile` and pre-flight now do, and any survivor is written up as a finding against Story 11.5.
- `tests/schemas/published-census.ts` read against the run, with each of the six constants recorded here beside the story that last moved it.
- The six banned constructions grepped across every file this story edited, with each surviving hit read and kept only when both halves carry a fact.
